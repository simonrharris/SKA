#ifndef __SK_COMPARE_HPP_INCLUDED__
#define __SK_COMPARE_HPP__
#endif

#include <string> //std::string
#include <vector> //std::vector

using namespace std;

int compareKmerFiles(string queryfile, vector<string> subjectfiles);